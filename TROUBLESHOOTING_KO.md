# 문제 해결

## 원본 해시 불일치

패치를 적용하지 마십시오. 다른 리비전, 기존 패치 적용본 또는 재압축·변조 ISO일 수 있습니다.
지원 원본 SHA-256은 다음 값입니다.

```text
5c23bbf4bb0415edb04b00b3cadbcfa9c1d6fd776a5ef838408ccda682f44cc5
```

## `source file too short`, `target window checksum mismatch` 또는 유사 오류

대부분 원본 ISO가 다르거나 패치 파일이 손상된 경우입니다.

1. 원본 크기와 SHA-256을 확인합니다.
2. 패치 크기가 `43,691,745 bytes`인지 확인합니다.
3. 패치 SHA-256이 `fc5e0c50d425b5a2f475c2cf2d0164dd66ba57d3effe6f4ea26c17a95d411587`인지 확인합니다.
4. 새 출력 파일명으로 다시 적용합니다.

## xdelta3를 찾지 못함

`xdelta3`를 PATH에 등록하거나 자동 적용 스크립트에 실행 파일 경로를 전달하십시오.

```powershell
python scripts/apply_patch.py "원본.iso" --xdelta "C:\Tools\xdelta3.exe"
```

## 출력 파일을 만들 수 없음

출력 위치에 약 2 GB의 추가 여유 공간과 쓰기 권한이 있는지 확인하십시오. 기존 출력 파일이 있으면
다른 이름을 사용하거나, 내용을 확인한 후 자동 적용 스크립트의 `--force` 옵션을 사용하십시오.

## 패치 결과 해시 불일치

정상 결과 SHA-256은 다음 값입니다.

```text
f64044662f58e7e46df4a75c83ba88f82bae93386d7ef7713c7aef8574ca715b
```

자동 적용 스크립트는 결과가 일치하지 않으면 임시 파일을 제거하고 최종 파일로 남기지 않습니다.

## 한글 이미지가 이전 버전처럼 보이거나 일본어가 다시 보임

과거 PPSSPP 세이브스테이트에는 이전 VRAM 텍스처가 남을 수 있습니다. 세이브스테이트를 불러오지
말고 ISO를 완전히 새로 부팅하십시오. 일반 게임 저장 데이터와 세이브스테이트는 서로 다릅니다.

## 글자가 흐리거나 지나치게 작아 보임

PPSSPP의 선형 필터링, 후처리 셰이더와 비정수 배율을 끄고 정수 배율·최근접 보간으로 비교하십시오.
게임 내 한글 텍스트 글꼴은 원본 렌더러의 8×8 셀 규격을 유지합니다.

## 타이틀 로고가 일본어임

소유자 검수에서 타이틀 로고를 원본 유지하기로 결정한 의도된 상태입니다. 타이틀 이외의 게임 진행
화면에서 일본어가 확인되면 스크린샷과 재현 경로를 포함해 제보하십시오.

## 대사 행 또는 배열이 깨져 보임

먼저 새로 부팅했는지 확인하십시오. 동일 현상이 재현되면 이벤트인지 보이스인지, 해당 화면까지의
진행 순서, PPSSPP 버전·그래픽 설정과 개인정보를 가린 스크린샷을 [지원 안내](SUPPORT_KO.md)에
따라 제보하십시오. ISO나 세이브스테이트는 첨부하지 마십시오.
